# Мини-квесты отголосков (GDD §1.4 п.6): шесть просьб «принести, зажечь, найти».
# Награда за каждую — перо (world_t.feathers), перья открывают бонусный остров и второй финал.
# Квесты необязательны и не блокируют прохождение.
#
# Сборка: python3 tools/questgen.py build/assets → build/assets/quests_data.h
# (таблица `static const quest_def_t QUESTS[]` и `#define QUEST_COUNT`).
# Состояние квеста нигде не хранится: оно выводится из флагов мира (core/quest.c).
#
# Поля [[quest]] (все обязательны, лишних ключей быть не должно):
#   name        — служебное имя, попадает только в комментарий сгенерированного заголовка
#   giver       — id сущности-отголоска (ENT_ECHO) на уровне; 1..199, уникален по всей таблице
#   need_flag   — id сущности, сбор или активация которой и есть выполнение просьбы (флаг 1..199)
#   accept_flag — глобальный флаг «просьба принята» (204..255)
#   reward_flag — глобальный флаг «просьба закрыта, перо выдано» (204..255)
#   str_offer / str_progress / str_done — идентификаторы строк из assets/strings.csv без STR_
#
# Распределение id сущностей по островам. Флаги мира 1..199 — общие для всей игры
# (core/world.h), поэтому одинаковые id на разных островах делили бы один флаг:
#   хаб «Веко»        1..49
#   Сад Зеркал      110..119
#   Часовые Террасы  120..129
#   Тихая Вода       130..139
#   Библиотека Сна   140..149
# Глобальные флаги: 200..203 заняты WFLAG_* (core/world.h), квесты берут по два подряд
# из 208..219 — для квеста N (1..6): accept = 206 + 2·N, reward = 207 + 2·N.
#
# Строки: в assets/strings.csv пока есть только ECHO_1..ECHO_3, поэтому у квестов 2..6
# реплики временные (см. TODO у каждого) — идентификаторы на замену перечислены в отчёте.

# --- хаб «Веко»: два отголоска у площадки (levels/hub.toml, id 10 и 11) ---

# «Найти»: отголосок помнит форму глаза, но не помнит, где он лежит.
# Условие — малый глаз в восточном закоулке хаба (hub.toml, SMALL_EYE id 30).
[[quest]]
name = "hub_east_eye"
giver = 10
need_flag = 30
accept_flag = 208
reward_flag = 209
str_offer = "ECHO_1"
str_progress = "ECHO_2"
str_done = "ECHO_3"

# «Принести»: просит отдать ему второй найденный глаз, чтобы «снова увидеть имя».
# Условие — малый глаз в западном закоулке хаба (hub.toml, SMALL_EYE id 31).
[[quest]]
name = "hub_west_eye"
giver = 11
need_flag = 31
accept_flag = 210
reward_flag = 211
str_offer = "ECHO_1"      # TODO(strings): ECHO_Q2_OFFER
str_progress = "ECHO_2"   # TODO(strings): ECHO_Q2_WAIT
str_done = "ECHO_3"       # TODO(strings): ECHO_Q2_DONE

# --- Сад Зеркал: «зажечь» ---

# Отголосок сада просит довести луч до приёмника у его камня.
# Условие — приёмник луча (RECEIVER id 119 на острове сада).
[[quest]]
name = "mirrors_light_receiver"
giver = 110
need_flag = 119
accept_flag = 212
reward_flag = 213
str_offer = "ECHO_1"      # TODO(strings): ECHO_Q3_OFFER
str_progress = "ECHO_2"   # TODO(strings): ECHO_Q3_WAIT
str_done = "ECHO_3"       # TODO(strings): ECHO_Q3_DONE

# --- Часовые Террасы: «найти путь» ---

# Отголосок террас просит подняться туда, куда лестница ведёт только при одном повороте.
# Условие — невидимый триггер на верхней площадке (TRIGGER id 129).
[[quest]]
name = "terraces_reach_top"
giver = 120
need_flag = 129
accept_flag = 214
reward_flag = 215
str_offer = "ECHO_1"      # TODO(strings): ECHO_Q4_OFFER
str_progress = "ECHO_2"   # TODO(strings): ECHO_Q4_WAIT
str_done = "ECHO_3"       # TODO(strings): ECHO_Q4_DONE

# --- Тихая Вода: «принести» ---

# Отголосок воды просит привести плавучий блок на плиту у затопленного края.
# Условие — нажимная плита (PLATE id 139).
[[quest]]
name = "water_float_to_plate"
giver = 130
need_flag = 139
accept_flag = 216
reward_flag = 217
str_offer = "ECHO_1"      # TODO(strings): ECHO_Q5_OFFER
str_progress = "ECHO_2"   # TODO(strings): ECHO_Q5_WAIT
str_done = "ECHO_3"       # TODO(strings): ECHO_Q5_DONE

# --- Библиотека Сна: «вспомнить» ---

# Отголосок библиотеки просит повторить порядок глаз в зале на панели памяти.
# Условие — панель памяти (MEMORY_PANEL id 149).
[[quest]]
name = "library_memory_panel"
giver = 140
need_flag = 149
accept_flag = 218
reward_flag = 219
str_offer = "ECHO_1"      # TODO(strings): ECHO_Q6_OFFER
str_progress = "ECHO_2"   # TODO(strings): ECHO_Q6_WAIT
str_done = "ECHO_3"       # TODO(strings): ECHO_Q6_DONE
